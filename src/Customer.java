// Customer interface that defines the methods for a Customers.
public interface Customer {
    // Method to update the customer about a new model.
    void update(String modelName);
    // Method to get the name of the customer.
    String getName();
}